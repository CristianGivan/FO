package com.app.FO.util;

import com.app.FO.dto.note.ShowNoteDTO;
import com.app.FO.dto.note.ShowTagShortDTO;
import com.app.FO.model.note.Note;
import com.app.FO.model.note.NoteTag;
import com.app.FO.model.note.Tag;

import java.util.List;

public class ConvertToDTO {
//    private NoteTag
//
//    public static ShowNoteDTO showNoteDTOConvertedFromNote(Note note){
//
//        return new ShowNoteDTO(note.getId(),note.getNote(),
//                showNoteDTOConvertedFromNote(note.getNoteTags().),)
//    }
//
//    private List<ShowTagShortDTO> convertListTagsToShowTagsShortDTO(List<Tag> tags){
//        return tags.stream()
//                .map(tag -> convertTagToShowTagShortDTO(tag))
//                .toList();
//    }
//    private ShowTagShortDTO convertTagToShowTagShortDTO(Tag tag){
//        return new ShowTagShortDTO(tag.getId(), tag.getTagName());
//    }
}
