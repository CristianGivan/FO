Methods
Naming:
From sau For
By

Service se va returna doar elementele de baza exemplu Note
In Controller se va returna Ce DTO se doreste


Getting DTO in Controle (in service nu se va face verificare pentru ca am putea sa returnam null )
    1 find what to covert (prima lime de cod find note)
    2. if is null throw exception
    3. convert (return convert note to noteDTO)


Updating
Mapper
- cand adaug un DTO intr-un alt DTO
    - Cand adaug un nou DTO intr-un alt DTO sa nu uit sa adaug si clasa la uses
        - un User @Mapper(componentModel = "spring", uses = {UserRoleDTOMapper.class,UserTagDTOMapper.class})
    - daca nu nu mi afiseaza datele din DTO corect
